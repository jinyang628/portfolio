import NotesSideBarWrapper from '@/components/notes/side-bar-wrapper';

export default function Notes() {
  return (
    <NotesSideBarWrapper>
      <div className="flex flex-col items-center justify-center"></div>
    </NotesSideBarWrapper>
  );
}
